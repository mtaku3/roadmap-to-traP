import { {{{Name}}} } from "./Entity";
import { {{{Name}}}Id } from "./Identifier";

export interface I{{{Name}}}Repository {
  findById(id: {{{Name}}}Id): Promise<{{{Name}}} | undefined>;
  exist(id: {{{Name}}}Id): Promise<boolean>;
  save({{{name}}}: {{{Name}}}): Promise<void>;
  delete(id: {{{Name}}}Id): Promise<void>;
}
